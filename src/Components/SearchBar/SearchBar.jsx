import React from 'react';
import './Search.css';

const SearchBar = () => {
  return (
    <input
      type="text"
      className="search-input"
      placeholder="Search..."
    />
  );
};

export default SearchBar;
